mod stats {
    use serde::{Deserialize, Serialize};
    #[derive(Serialize, Deserialize, Debug)]
    struct Stats {
        below_two: bool,
        unused_points: isize,
        intellegence: isize,
        reflex: isize,
        tech: isize,
        cool: isize,
        attractiveness: isize,
    }
    impl Stats{
        fn Getmut
    }
}
